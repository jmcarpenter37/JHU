/**
 * @author John Carpenter
 * The suits are enum types.
 * Enum types are nice because we can use them to hard code values to each enum.
 * See: https://docs.oracle.com/javase/tutorial/java/javaOO/enum.html
 * for more information
 */
public enum CardSuit
{
    // Define the suits you find in a deck of cards
    DIAMONDS,
    CLUBS,
    HEARTS,
    SPADES
}
