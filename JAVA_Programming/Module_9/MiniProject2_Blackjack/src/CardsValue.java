/**
 * @aythor John Carpenter
 * Enum types are nice because we can use them to hard code values to each enum.
 */
public enum CardsValue
{
    /* Define the values of each card.
    The scores will get handled later on.
    */
    ACE,
    TWO,
    THREE,
    FOUR,
    FIVE,
    SIX,
    SEVEN,
    EIGHT,
    NINE,
    TEN,
    JACK,
    QUEEN,
    KING,
}
